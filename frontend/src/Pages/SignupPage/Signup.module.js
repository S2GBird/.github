const sxStyle = {
  display: 'flex',
  height: 'auto'
}

const image = {
  display: 'flex',
  width: '100%',
  height: '100%',
  objectFit: 'cover'
}

const gridStyle = {
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  justifyContent: 'center',
  marginLeft: '75px'
}

const formContainer = {
  marginTop: '20px',
  alignItems: 'center',
  justifyContent: 'center'
}

const inputLabelStyle = {
  fontWeight: 'bold'
}

const signinButton = {
  color: 'black',
  fontWeight: 'bold',
  marginTop: -5
}

const buttonContainer = {
  display: 'flex',
  alignItems: 'center'
}

const registerButton = {
  color: 'black',
  fontWeight: 'bold',
  backgroundColor: '#1B7D4F',
  marginLeft: '125px',
  marginTop: '10px',
  width: '300px'
}

const buttonContainer2 = {
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  justifyContent: 'center'
}

export { sxStyle, image, gridStyle, formContainer, inputLabelStyle, signinButton, buttonContainer, registerButton, buttonContainer2 }
